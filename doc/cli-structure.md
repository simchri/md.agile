# CLI Structure Overview

A tree-like reference of every binary, subcommand, and flag in this repo,
with implementation status. Generated by reading the current `clap`
definitions (`crates/cli/src/cli/mod.rs` and friends) and cross-referencing
planned-but-unbuilt items from `tasks.agile.md`.

Legend: ✅ implemented · 🚧 planned (not yet implemented) · ⚠️ implemented but
tracked for change/removal.

```
agile                                          ✅ binary (crates/cli/src/main.rs)
│
├── (no subcommand)                            ✅ opens the next active top-level
│                                                  task in $VISUAL/$EDITOR
│                                                  (subcommands/default.rs)
│
├── list [tasks]                                ✅ prints tasks from all *.agile.md
│   │                                              files in priority order
│   ├── -n, --next <COUNT>                      ✅ show only the first COUNT entries
│   ├── --last <COUNT>                          ✅ show only the last COUNT entries
│   └── -a, --all                                ✅ include done/cancelled tasks
│
├── list files                                  ✅ list recognised task files,
│   │                                              in priority order
│   ├── -n, --next <COUNT>                      ✅
│   └── --last <COUNT>                          ✅
│                                                  (no --all — files have no status)
│
├── task (alias: tasks)                         ✅ command group for task-centric
│   │                                              operations
│   │
│   ├── next                                     ✅ print the next incomplete
│   │                                                top-level task (+ subtasks)
│   │   ⚠️ no flags/args today; tasks.agile.md still lists
│   │      "remove unused flag --next N" as a #ctd-cli-commands item —
│   │      that flag no longer exists in the code, so this line is stale
│   │
│   ├── next <N>                                🚧 planned: show the next N
│   │                                              incomplete tasks (positional
│   │                                              arg, replaces any flag-based
│   │                                              count)
│   ├── next <I.J>                              🚧 planned: address a specific
│   │                                              (sub)task by position, e.g.
│   │                                              `1.1` = first subtask of the
│   │                                              next task, `2.2` = second
│   │                                              sub-subtask of the second task
│   ├── done <I.J>                              🚧 planned: mark the addressed
│   │                                              (sub)task done; must still
│   │                                              enforce completion rules
│   │                                              (e.g. incomplete children)
│   │                                              and print an error instead of
│   │                                              silently failing; should be
│   │                                              efficient (not a full-project
│   │                                              re-check)
│   └── next --mine                              🚧 planned: show the next task
│                                                    this identity is eligible to
│                                                    take, using the same
│                                                    eligibility rules as
│                                                    assignment/completion
│                                                    validation (E013)
│
└── check                                        ✅ validate all *.agile.md files
    │                                                against the built-in rule set
    │                                                (E001–E016, see doc/checks.md);
    │                                                prints "<path>:<line>: <msg>"
    │                                                per issue; exit 1 if any found
    ├── --as <USER>                              ✅ override the resolved identity
    │                                                for the E013 authorization
    │                                                check (CI use case)
    └── --base <REF>                              ✅ override the git ref used as
                                                     the "old" side of the E013
                                                     diff, instead of HEAD
                                                     (CI use case)

agilels                                         ✅ separate binary — the LSP server
│                                                   (crates/cli/src/bin/agilels.rs).
│                                                   Not a subcommand of `agile`;
│                                                   editors launch it directly over
│                                                   stdio JSON-RPC.
└── --version                                    ✅ print the crate version and exit
```

## Notes / open questions for review

- **Scope**: this overview covers the `agile` and `agilels` binaries only
  (the `mdagile` library crate's public CLI surface). The `crates/gui`
  binary (Dioxus board view) is a separate, non-CLI surface and isn't
  included here — flag if it should be.
- **Stale backlog item**: the `#ctd-cli-commands` bullet "remove unused flag:
  `agile task next --next N`" no longer matches the code — `task next`
  currently takes zero flags/args. Worth confirming with the human whether
  that bullet should just be deleted/reworded now that the flag is already
  gone, before the rest of the `task next <N>` / `task next <I.J>` / `task
  done <I.J>` / `task next --mine` work is scoped.
- **Addressing scheme for `task next <I.J>` / `task done <I.J>`**: not yet
  specified — needs a decision on what `I` and `J` count over (all tasks in
  priority order vs. per-file, whether cancelled/done tasks occupy a number,
  etc.) before implementation can start.
- **`task done <I.J>` efficiency requirement**: the backlog explicitly asks
  to avoid checking the whole project. Today `check` always re-parses and
  re-validates every `*.agile.md` file; a scoped single-task validation path
  doesn't exist yet and would be new design work, not a small tweak.
- **LSP feature surface** (diagnostics, hover, etc. — see `doc/config.md`
  and the "More LSP features" section of `tasks.agile.md`) is deliberately
  out of scope for this CLI-focused overview; it could get its own tree if
  useful.
